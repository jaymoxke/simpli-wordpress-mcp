# CI expectation

The draft pull request must trigger the existing pull-request workflow against `main`. Required jobs: dependency install, type/static checks, unit tests, WhatsApp gateway checks, build and runtime syntax check.
