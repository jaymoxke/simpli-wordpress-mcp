# Phase 1 scope lock

Phase 1 is limited to isolated repository foundation work. No production deployment, WordPress mutation, DNS change, plugin disablement/removal, OAuth credential rotation, or SDK major-version cutover is included.

Any such change requires a later verified tranche.
